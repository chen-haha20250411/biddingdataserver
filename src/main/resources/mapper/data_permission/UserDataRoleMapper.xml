<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xiao.core.data_permission.mapper.UserDataRoleMapper">

    <resultMap id="BaseResultMap" type="com.xiao.core.data_permission.domain.UserDataRole">
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="user_id" property="userId" jdbcType="INTEGER" />
        <result column="role_id" property="roleId" jdbcType="INTEGER" />
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP" />
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, role_id, created_at
    </sql>

    <select id="queryById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM
        user_data_role
        WHERE
        id = #{id}
    </select>

    <select id="queryByUserId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM
        user_data_role
        WHERE
        user_id = #{userId}
    </select>

    <select id="queryByRoleId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM
        user_data_role
        WHERE
        role_id = #{roleId}
    </select>

    <insert id="insert" parameterType="com.xiao.core.data_permission.domain.UserDataRole">
        <selectKey resultType="java.lang.Integer" keyProperty="id" order="AFTER">
            SELECT @@IDENTITY
        </selectKey>
        INSERT INTO user_data_role(
            user_id, role_id, created_at
        ) VALUES(
            #{userId}, #{roleId}, now()
        )
    </insert>

    <delete id="deleteById" parameterType="java.lang.Integer">
        DELETE FROM user_data_role WHERE id = #{id}
    </delete>

    <delete id="deleteByUserId" parameterType="java.lang.Integer">
        DELETE FROM user_data_role WHERE user_id = #{userId}
    </delete>

    <delete id="deleteByUserIdAndRoleId">
        DELETE FROM user_data_role 
        WHERE user_id = #{userId} AND role_id = #{roleId}
    </delete>

</mapper>
